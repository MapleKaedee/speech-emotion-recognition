[server]
# Cegah Streamlit hang saat stop — file watcher transformers butuh torchvision
fileWatcherType = "none"

[browser]
gatherUsageStats = false

[theme]
primaryColor = "#fafafa"
backgroundColor = "#0a0a0a"
secondaryBackgroundColor = "#171717"
textColor = "#fafafa"
font = "sans serif"